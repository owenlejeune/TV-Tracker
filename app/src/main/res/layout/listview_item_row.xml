<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/show_title_row_label"
        app:layout_constraintStart_toStartOf="parent"
        android:textSize="@dimen/text_size"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/air_date_label"
        app:layout_constraintEnd_toEndOf="parent"
        android:textSize="@dimen/text_size"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/next_episode_label"
        app:layout_constraintStart_toStartOf="@id/show_title_row_label"
        app:layout_constraintEnd_toEndOf="@id/air_date_label"
        android:textSize="@dimen/text_size"/>



</android.support.constraint.ConstraintLayout>